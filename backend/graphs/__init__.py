"""LangGraph state machines for stateful, branching AI workflows.

Currently empty — the first graph (``preparation_graph``) lands in Phase 7C.
Audio FSM, narration interruption flow, and other low-level orchestration
remain hand-rolled in :mod:`backend.services.session_service` — LangGraph
is reserved for higher-level study-pipeline orchestration.
"""
